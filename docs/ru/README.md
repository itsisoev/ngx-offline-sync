# ngx-offline-sync

<div>
  <img src="https://img.shields.io/npm/dt/ngx-offline-sync" />
  <img src="https://img.shields.io/npm/v/ngx-offline-sync" />
  <img src="https://img.shields.io/github/stars/itsisoev/ngx-offline-sync" />
</div>

Библиотека для Angular, которая автоматически сохраняет HTTP-запросы при отсутствии интернет-соединения и синхронизирует их после восстановления сети.

**Документация:** [English](../../README.md) · Русский · [日本語](../ja/README.md)

## Содержание

- [Возможности](#возможности)
- [Установка](#установка)
- [Подключение](#подключение)
- [Использование](#использование)
- [Как это работает](#как-это-работает)
- [Статусы запросов](#статусы-запросов)
- [Повторные попытки](#повторные-попытки)
- [Архитектура](#архитектура)
- [Планы развития](#планы-развития)
- [Ограничения](#ограничения)
- [Лицензия](#лицензия)

## Возможности

- Автоматическая постановка HTTP-запросов в очередь при отсутствии сети
- Сохранение очереди в IndexedDB
- Автоматическая синхронизация после восстановления соединения
- Последовательная обработка запросов
- Повторные попытки выполнения неуспешных запросов
- Управление статусом каждого запроса
- Поддержка `POST`, `PUT`, `PATCH` и `DELETE`

## Установка

```bash
npm install ngx-offline-sync
```

## Подключение

Добавьте `provideOfflineSync()` и `offlineSyncInterceptor` в конфигурацию приложения:

```ts
import { ApplicationConfig } from '@angular/core';

import {
  provideHttpClient,
  withInterceptors,
} from '@angular/common/http';

import {
  provideOfflineSync,
  offlineSyncInterceptor,
} from 'ngx-offline-sync';

export const appConfig: ApplicationConfig = {
  providers: [
    provideOfflineSync(),

    provideHttpClient(
      withInterceptors([
        offlineSyncInterceptor,
      ]),
    ),
  ],
};
```

После подключения библиотека самостоятельно управляет очередью, хранилищем, синхронизацией и повторными попытками. Дополнительная регистрация внутренних сервисов не требуется.

## Использование

После подключения библиотека не требует специального API для отправки запросов. Используйте обычный `HttpClient`:

```ts
this.http.post('/api/products', product).subscribe();
```

- При наличии интернет-соединения запрос выполняется обычным способом.
- Если соединение отсутствует, запрос автоматически помещается в очередь.

## Как это работает

### Интернет доступен

```
HttpClient
    ↓
offlineSyncInterceptor
    ↓
HTTP request
    ↓
Server
```

Запрос отправляется напрямую.

### Интернет отсутствует

```
HttpClient
    ↓
offlineSyncInterceptor
    ↓
QueueService
    ↓
IndexedDB
    ↓
PENDING
```

Запрос сохраняется локально и ожидает восстановления соединения.

### Интернет восстановлен

```
NetworkStatusService
    ↓
SyncCoordinatorService
    ↓
SyncService
    ↓
SYNCING
    ↓
HTTP request
    ↓
COMPLETED
```

Очередь обрабатывается автоматически.

## Статусы запросов

Каждый запрос имеет один из следующих статусов:

| Статус | Описание |
|---|---|
| `PENDING` | Запрос ожидает выполнения |
| `SYNCING` | Запрос выполняется |
| `COMPLETED` | Запрос успешно выполнен |
| `FAILED` | Запрос не удалось выполнить |

Основной жизненный цикл запроса:

```
PENDING → SYNCING → COMPLETED
```

При ошибке:

```
SYNCING → RetryPolicy → PENDING → Retry
```

Если запрос больше не должен повторяться:

```
SYNCING → FAILED
```

## Повторные попытки

Если синхронизация завершается ошибкой, библиотека передаёт ошибку в `RetryPolicy`.

Политика определяет:

- нужно ли повторить запрос;
- задержку перед следующей попыткой;
- когда запрос должен получить статус `FAILED`.

Общий жизненный цикл:

```
SYNCING → Request failed → RetryPolicy → PENDING → Retry
```

После успешного выполнения:

```
PENDING → SYNCING → COMPLETED
```

Если количество попыток превышает допустимое:

```
SYNCING → Request failed → FAILED
```

## Архитектура

Библиотека разделена на несколько основных компонентов:

```
NetworkStatusService
        │
        ▼
OfflineSyncInterceptor
        │
        ▼
QueueService
        │
        ▼
IndexedDbStorage
        │
        ▼
SyncCoordinatorService
        │
        ▼
SyncService
        │
        ├── RetryPolicy
        │
        └── HttpClient
```

### Основные компоненты

**offlineSyncInterceptor**
Перехватывает HTTP-запросы и определяет, необходимо ли поместить запрос в очередь.

**NetworkStatusService**
Отслеживает состояние интернет-соединения и сообщает библиотеке о восстановлении сети.

**QueueService**
Управляет очередью запросов: добавлением, получением, обновлением и удалением элементов.

**IndexedDbStorage**
Использует IndexedDB для постоянного локального хранения очереди.

**SyncCoordinatorService**
Отслеживает восстановление соединения и запускает синхронизацию очереди.

**SyncService**
Получает запросы из очереди, выполняет их через `HttpClient` и обновляет их статус.

**RetryPolicy**
Определяет правила повторного выполнения неуспешных запросов.

## Планы развития

Проект находится в активной разработке. Планируемое развитие библиотеки включает:

- Поддержку настраиваемых стратегий повторных попыток
- Расширенную настройку очереди запросов
- Управление приоритетом запросов
- Возможность отмены и удаления запросов из очереди
- Расширенное управление состоянием синхронизации
- Улучшение обработки ошибок и сетевых состояний
- Расширенные возможности настройки IndexedDB
- Дополнительные инструменты для мониторинга очереди
- Расширение тестового покрытия
- Улучшение документации и примеров использования

## Ограничения

- На текущий момент библиотека предназначена для синхронизации следующих HTTP-методов: `POST`, `PUT`, `PATCH`, `DELETE`.
- `GET`-запросы не помещаются в offline queue.
- Очередь хранится локально в IndexedDB браузера.

## Лицензия

См. [LICENSE](../../LICENSE).
